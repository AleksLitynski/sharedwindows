-- dont hardcode link
-- make "now posting" appear
fix for iPhone


solutions:
- ignore node_modules. + gets rid of problem. - each use has to install all deps. No future safety
- merge win and mac versions of node_module. + easier + backwards comp + could be slower - 
- incude copy of dep repos for future, but don't install by default

